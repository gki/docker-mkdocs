services:
    - docker

before_install:
    - docker build -t dockertest .

script:
    # installed lib check
    - docker run dockertest mkdocs --version
    - docker run gentkd/extended-mkdocs weasyprint --version
    # build mkdocs
    # build with mkdocs
